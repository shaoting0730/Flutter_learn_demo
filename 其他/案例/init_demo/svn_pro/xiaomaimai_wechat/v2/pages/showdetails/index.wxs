module.exports = {
  isShowAddBtn: function (guid, cartlist) {

    var isShow = true;
    cartlist.forEach(function (item) {
      if (item.ProductCode === guid) {
        isShow = item.Quantity + 1 <= item.StockQuantity

        return false
      }
    })
    return isShow
  }
}
